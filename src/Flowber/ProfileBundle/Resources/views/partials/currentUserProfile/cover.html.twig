<div class="jumbotron generic-cover" style="background-image: url('{{asset(user.coverPicture)}}')">
    <div class="container" >
        <div class="row">
            <div class="generic-cover-profile-picture img-circle" style="background-image: url('{{asset(user.profilePicture)}}')"></div> 
            <div class="generic-cover-title">
                <h1 class="generic-cover-title-name">{{ user.name }} </h1>
                <p class="generic-cover-title-description">{{ user.subtitle }}</p>
            </div>
            <div class="generic-cover-edit-button">
                <a href="{{ path('flowber_profile_edit') }}" class="btn btn-default">Modifier le profil</a>
            </div>
        </div>
    </div>
</div>    