<nav class="navbar navbar-default navbar-home">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#content-navbar-main" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('flowber_front_office_signin_homepage') }}">                  
                <span class="site-name">FlowBer</span>
            </a>
        </div>
        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="content-navbar-main">
            <form method="post" action="{{ path("fos_user_security_check") }}" class="navbar-right navbar-form navbar-connection-form">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                <table>                    
                    <tr>
                        <td>
                            <label class="sr-only" for="connection-email">Email address</label>
                            <input class="form-control navbar-connection-form-input" type="email" id="username" name="_username" value="{{ last_username }}" required="required" placeholder="Adresse e-mail" />
                        </td>
                        <td>
                            <label class="sr-only" for="connection-password">Password</label>
                            <input type="password" class="form-control navbar-connection-form-input" id="password" name="_password" required="required" placeholder="Mot de passe"/>
                        </td>
                        <td>
                            <input type="submit" class="btn btn-default navbar-connection-form-submit" id="_submit" name="_submit" value="Connexion" />
                        </td>
                    </tr>
                    <tr>
                        <td>
                            <div class="checkbox navbar-connection-form-keep">
                                <label>
                                        <input type="checkbox" id="remember_me" name="_remember_me" value="on" checked="checked" />
                                        Maintenir la connexion
                                </label>
                            </div>
                        </td>
                        <td class="text-right">
                            <div class="navbar-connection-form-passforgotten">
                                <a href="#">Mot de passe oublié</a>
                            </div>
                        </td>
                    </tr>
                </table>
            </form>
        </div><!-- /.navbar-collapse -->
        <p class="navbar-background-text">Connectez-vous !</p>
    </div><!-- /.container-fluid -->
</nav>