{# empty Twig template #}
{% extends '::base_frontoffice.html.twig' %}

{% block stylesheets %}
    {{parent()}}
    <link rel="stylesheet" href="{{ asset('assets/css/EventBundle/generic-event.css') }}"/> 
    <link rel="stylesheet" href="{{ asset('assets/css/GroupBundle/generic-group.css') }}"/> 
    <link rel="stylesheet" href="{{ asset('assets/css/ProfileBundle/generic-profile.css') }}"/> 
    <link rel="stylesheet" href="{{ asset('assets/css/SearchBundle/search.css') }}"/> 
{% endblock %}

{% block smallMenu %}
    {{parent()}}
    <!-- BEGIN small screen menu -->

    <!-- END small screen menu -->
{% endblock %}

{% block body %}

    <!-- navbar top -->
    {% include 'FlowberFrontOfficeBundle::partials/common/main-connected-navbar.html.twig' %}
    
    <div class="container">
        <div class="results-all">
            <h2>Résultats de la recherche ({{(events|length) + (groups|length) + (friends|length)}})</h2>

            <div class="results-group">
                <h3>Sorties</h3>
                <div class="row">
                    <div class="event-thumbnail-display-container">
                    {% for event in events %}
                       {% include 'FlowberEventBundle::partials/oneEventResume.html.twig' %}     
                    {%endfor%}
                    </div>
                </div>
                <h3>Groupes</h3>
                <div class="row">
                    <div class="group-thumbnail-display-container">
                    {% for group in groups %}
                       {% include 'FlowberGroupBundle::partials/oneGroupResume.html.twig' %}     
                    {%endfor%}
                    </div>
                </div>
                <h3>Profiles</h3>
                <div class="row">
                    <div class="generic-profile-list">
                    {% for friend in friends %}
                       {% include 'FlowberProfileBundle::partials/oneProfileResume.html.twig' %}     
                    {%endfor%}
                    </div>
                </div>
            </div>
        </div>
    </div>
   
    <!-- footer -->                  
    {% include 'FlowberFrontOfficeBundle::partials/common/footer.html.twig' %}
    
{% endblock %}

{% block javascripts %}
    {{parent()}}
    {# Put specific JS below #}

    

{% endblock %}